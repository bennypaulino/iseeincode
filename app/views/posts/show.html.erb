<hr id="headerBottomBorder" class="clear">

<div class="post_show_content skinny_wrapper wrapper_padding">
  <header>
    <p class="date"><%= @post.created_at.strftime('%A, %B %d') %></p>
    <h1><%= @post.title %></h1>
    <hr>
  </header>

  <div id="content">
    <%= markdown @post.content %>
  </div>

  <hr>

  <div id="share_box">
    <p>Share<p>
    <!-- Twitter -->
    <a onclick="window.open('http://twitter.com/share?text=<%= @post.title %> by @i_see_in_code- &amp;url=<%= %>', '_blank','width=800,height=500,top=200,left=300');void(0);" ><i class="fa fa-twitter"></i></a>

    <!-- Facebook -->
    <a onclick="window.open('http://facebook.com/sharer.php?u=<%= @post.title %> <%= %>',' _blank','width=800,height=500,top=200,left=300');void(0);" ><i class="fa fa-facebook"></i></a>

    <!-- Reddit-->
    <a href="#"><i class="fa fa-reddit"></i></a>

    <!-- Google+ -->
    <a onclick="window.open('https://plus.google.com/share?url=<%= @post.title %> <%= %>', '_blank','width=800,height=500,top=200,left=300');void(0);" ><i class="fa fa-google-plus"></i></a>
  </div>

  <!-- 
  There is a ruby code snippet that has been cut out (lines 22 after the &amp;url=). Putting it in place results in a NoMethodError undefined mehtod `model_name' for Hash:Class
  paste in the following:
  url_for([@post, {only_path: false}])

  FIX THE SAME ISSUE FOR Google+ Facebook Reddit
 -->

  <hr>

<%= render 'disqus' %>

</div>



<br>
<br>

<%= link_to 'Edit', edit_post_path(@post) %>
<%= link_to 'Delete', post_path(@post), method: :delete, data: { confirm: 'Are you sure you want to delete this post?' } %>